# Hindari opsi deprecated `production` (memicu npm warn di Railway).
# Saat NODE_ENV=production / CI=true, npm bisa meng-omit dev secara default.
# Hanya omit paket optional — devDependencies tetap terpasang untuk `next build`.
omit=optional
