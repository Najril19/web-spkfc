# Hindari peringatan deprecated "Use `--omit=dev` instead" saat platform set NPM_CONFIG_PRODUCTION.
# Build Next perlu devDependencies (TypeScript, ESLint, dll.) — jangan pakai install prod-only sebelum `next build`.
production=false
