# web-spkfc

Next.js + Supabase — MJMScan+ (diagnosa kerusakan mobil Toyota Avanza).

Setup: salin `.env.example` ke `.env.local`, jalankan `supabase/migrations/001_schema.sql` di Supabase SQL Editor, lalu `npm install` dan `npm run dev`.
